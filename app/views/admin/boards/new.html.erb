<%= render :partial => "common/user_nav" %>

<%= form_for @board, :url => admin_boards_path(@board) do |f| %>
    <%= f.label :name, "Board Name:" %>
    <%= f.text_field :name %>
    <%= f.submit "Create" %>
<% end %>