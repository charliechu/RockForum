<%= render :partial => "common/user_nav" %>

<%= form_for @board, :url => admin_board_path(@board), :method => :put do |f| %>
    <%= f.label :name, "Board Name:" %>
    <%= f.text_field :name %>
    <%= f.submit "Update" %>
<% end %>